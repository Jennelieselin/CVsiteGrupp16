@model Shared.Models.CvIndex

<table class="table">
    @foreach (var item in Model.ListOfErfarenhet)
     {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Namn)
        </td>
        <td>
            @Html.ActionLink("Redigera erfarenhet", "Edit", "Erfarenhet", new { id = item.Id }, null) |
            @Html.ActionLink("Radera erfarenhet", "Delete", "Erfarenhet", new { id = item.Id }, null)
        </td>
    </tr>
     }
</table>